[toolchain]
channel    = "nightly"
# llvm-tools provides llvm-objcopy for `cargo objcopy` (raw firmware image
# for the UART/BLE update path and tools/fw-upload).
components = ["rust-src", "llvm-tools"]
targets    = ["thumbv7em-none-eabi"]
